# 다음 배포 메모

## 2026-09-08 예정: 마을 꾸미기 데이터 복사

2026-09-07 사용자 요청: 내일 배포할 때 로컬에서 놓은 펜스, 새로 지은 집,
새로 칠한 바닥을 운영 서버에 빠짐없이 복사한다. 배포가 미뤄지면 다음 배포까지
이 항목을 유지한다. 현재는 메모만 작성했으며 운영 서버에 복사하지 않았다.

- [ ] **펜스**: 실제 `STATE_DIR/game_data.db`의 `land_fences`에서 이번에 설치한
  펜스를 확인하고 운영 DB에 선별 이관한다. 기본 DB 경로는 `data/game_data.db`다.
  펜스 위치는 지형 파일이나 GLB에 들어 있지 않으므로 지형 동기화만으로 복사되지 않는다.
  운영 DB를 백업하고, `estate_id` 및 소유지 밖 관리자 펜스의 `installer_id`를
  운영 서버의 올바른 영지·캐릭터 ID에 연결한다. 로컬 DB 전체로 운영 DB를 덮어쓰지 않는다.
- [ ] **새 집**: `data/housing/`의 추가·수정 JSON을 운영과 비교해 복사한다.
  운영에서만 바뀐 집을 보존하고, 관련 가구·침대 등 `data/terrain/objects/` 변경도 확인한다.
- [ ] **새 바닥**: `data/terrain/splat/`와 `data/terrain/landscaping/` 변경을 모두 확인해
  복사한다. 함께 편집한 높이·풀·나무·구역 데이터도 지형 동기화 후보에서 확인한다.
  `REMOTE=prod bash tools/sync-terrain.sh`로 먼저 차이를 확인한다.
- [ ] **Rowan**: 낮 위치 `(-1450.0, 1.1, 4761.8)`, 회전 `-91.7°`, 밤 71번 침대,
  뒷마당 바닥 샘플 안내가 반영됐는지 확인한다. 71번 침대 배치 데이터도 함께 확인한다.
- [ ] **배포 후 확인**: 펜스 위치·개수·충돌·회수, 새 집과 가구, 뒷마당 바닥 샘플을
  운영에서 확인하고, 복사한 파일·펜스 범위와 확인 결과를 이 메모에 기록한다.

집·지형·DB는 Git 배포만으로 전달되지 않는 운영 데이터다. 이번 사용자 요청의
펜스 이관은 일반 배포에서 개발 플레이어 상태를 옮기지 않는 원칙의 명시적 예외이며,
그 외 캐릭터·가방·골드·세금 상태는 이관 대상이 아니다.
